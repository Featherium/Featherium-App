{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main shell webview only",
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-greet"
  ]
}
